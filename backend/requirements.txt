# Upper bound is deliberate. FastAPI 0.141 stopped flattening include_router
# into APIRoute objects on app.routes, which makes the authorization
# inventory in tests/test_authz_inventory.py walk an empty route table and
# pass while proving nothing. The suite now fails loudly on that (see
# _registered_routes_checked); lift this bound only together with a fix to
# that introspection. Known-good: 0.136.3.
fastapi>=0.100.0,<0.137
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
feedparser>=6.0.0
pytest>=7.4.0
httpx>=0.24.0
python-dotenv>=1.0.0
anthropic>=0.25.0
google-genai>=1.0.0
beautifulsoup4>=4.12.0
tzdata>=2024.1
argon2-cffi>=23.1.0
# Pinned Hebrew language-frequency resource for anchor validation (#141). Offline,
# deterministic, no model/GPU/network at runtime. Pinned exactly for reproducibility.
wordfreq==3.1.1
